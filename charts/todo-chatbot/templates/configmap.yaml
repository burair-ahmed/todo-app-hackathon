apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-app-config
data:
  BACKEND_SERVICE_HOST: "{{ .Values.config.backendServiceHost }}"
  BACKEND_SERVICE_PORT: "{{ .Values.config.backendServicePort }}"
  NODE_ENV: "{{ .Values.config.nodeEnv }}"
  LOG_LEVEL: "{{ .Values.config.logLevel }}"